function variable() {
  // Variables are used to store values
  // The "let" keyword defines a variable that can be changed
  let x = 0
  // The value of x can be changed
  x = 5

  // The "const" keyword defines a variable whose value cannot be changed
  const y = 5

  // What will this print?
  console.log(x + y)
}

variable()
